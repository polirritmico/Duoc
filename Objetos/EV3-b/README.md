# README

Si se tiene Docker compose instalado en el sistema se puede leveantar la BD con:

```bash
docker compose up -d
```

Para ajustar la configuración de conección editar `application.properties`.
